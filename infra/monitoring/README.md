# infra/monitoring (placeholder)

Reserved for future Prometheus/Grafana (or equivalent) configuration once `GET /metrics` speaks the
Prometheus exposition format instead of the current plain-text rendering — see
`docs/architecture/overview.md` §8 (`infra::MetricsRegistry`) for the current state and the
intended swap point.
